{
  "name": "paper-source-reader",
  "version": "0.1.0",
  "private": true,
  "description": "PDF source alignment reader for Paper Visualizer",
  "main": "main.js",
  "scripts": {
    "configure": "electron configure-settings.js",
    "start": "electron .",
    "test": "node --test test/*.test.js && ../.venv/bin/python -m unittest discover -s python -p 'test_*.py'"
  },
  "dependencies": {
    "pdfjs-dist": "6.2.108"
  },
  "devDependencies": {
    "electron": "44.1.1"
  }
}
